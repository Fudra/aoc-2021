const utils = require('../utils');

const step01 = () => {
  const data = utils.openFile(utils.type.FILE_SAMPLE);

  let result = '';

  console.log(`result => ${result}`);
};

const step02 = () => {
  const data = utils.openFile(utils.type.FILE_SAMPLE);

  let result = '';

  console.log(`result => ${result}`);
};

step01();
//step02();
